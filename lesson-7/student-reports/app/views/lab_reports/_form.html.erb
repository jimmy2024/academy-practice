<%= form_with(model: lab_report) do |form| %>
  <% if lab_report.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(lab_report.errors.count, "error") %> prohibited this lab_report from being saved:</h2>

      <ul>
        <% lab_report.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.textarea :description %>
  </div>

  <div>
    <%= form.label :grade, style: "display: block" %>
    <%= form.select :grade, ["A", "B", "C", "D", "Fx", "F"] %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.collection_select(:user_id, User.all, :id, :full_name, prompt: "Выбрать Юзера") %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
